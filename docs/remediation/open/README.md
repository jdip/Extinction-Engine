# Open Remediations

Material follow-up work belongs here until it is implemented, cancelled with
rationale, or transferred to another planning system.

Create records with:

```powershell
./scripts/New-RemediationRecord.ps1 -Title "Title" -Owner "Owner" -Source "PR #123"
```
