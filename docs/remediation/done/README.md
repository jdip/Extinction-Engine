# Done Remediations

Closed, cancelled, and transferred remediation records move here.

Close records with:

```powershell
./scripts/Close-RemediationRecord.ps1 -Id "REM-YYYYMMDD-short-slug" -Resolution "What changed." -Validation "Evidence."
```
